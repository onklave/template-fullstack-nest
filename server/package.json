{
  "name": "template-fullstack-nest-api",
  "version": "0.1.0",
  "description": "Onklave template: NestJS + node-postgres API. Every route is mounted under /api.",
  "private": true,
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "node --watch --require ts-node/register src/main.ts",
    "test": "node --require ts-node/register --test test/*.test.ts",
    "architecture-test": "node --require ts-node/register --test test/architecture.test.ts",
    "e2e": "node --require ts-node/register --test test/governance.e2e.test.ts",
    "lint": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@nestjs/common": "^11.2.1",
    "@nestjs/core": "^11.2.1",
    "@nestjs/platform-express": "^11.2.1",
    "@onklave/app-runtime": "^0.1.1",
    "@onklave/errors": "^0.1.3",
    "pg": "^8.22.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.2.1",
    "@types/express": "^5.0.0",
    "@types/node": "^26.2.0",
    "@types/pg": "^8.21.0",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  }
}
