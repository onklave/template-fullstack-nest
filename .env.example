# Local development only. Copy to server/.env or export before `npm start`.
# In Onklave, DATABASE_URL is injected per environment as a secret — it is
# declared in onklave.yaml but its VALUE never lives in this repo.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
